name: test
on:
  push:
    branches:
      - productie
      - test
      - ontwikkel
  pull_request:
    branches:
      - productie
      - test
      - ontwikkel
jobs:
  # do-explore:
  #   name: Feedback runner
  #   runs-on: windows-2019
  #   steps:
  #     - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
  #     - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
  #     - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
  #     - name: Check out repository code
  #       uses: actions/checkout@v3
  #     - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
  #     - run: echo "🖥️ The workflow is now ready to test your code on the runner."
  #     - name: List files in the repository  
  #       run: |
  #         ls ${{ github.workspace }}
  #     - run: echo "🍏 This job's status is ${{ job.status }}."
  do-test:
    name: Test preproccesing scripts
    runs-on: windows-2019
    steps:
    - uses: actions/checkout@v2
    - name: Setup environment
      uses: goanpeca/setup-miniconda@v1
      with:
        environment-file: etc/actions_env.yml
        activate-environment: base
        python-version: 3.8
        auto-activate-base: false
    - run: conda info
    #- name: activate environment
    #  run: C:\Miniconda\condabin\conda.bat activate base
    - name: Run preprocessing BIO
      run: python Config/ModuleDataSetFiles/PreProImport_BIO.py tests/testfiles/Diatomeeen_test.csv
    - name: Run preprocessing FC
      run: python Config/ModuleDataSetFiles/PreProImport_FC.py tests/testfiles/FC_test.csv

    - name: Test preprocessing BIO + FC
      run: pytest tests
        
        